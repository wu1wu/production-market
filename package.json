{
  "name": "makerkit-play-contracts",
  "version": "1.0.0",
  "description": "MakerKit Play - Smart contracts for The Synthesis Hackathon",
  "scripts": {
    "compile": "npx hardhat compile",
    "deploy:sepolia": "npx hardhat run scripts/deploy.js --network baseSepolia",
    "deploy:base": "npx hardhat run scripts/deploy.js --network base",
    "test": "npx hardhat test"
  },
  "devDependencies": {
    "hardhat": "^2.22.0",
    "@nomicfoundation/hardhat-toolbox": "^5.0.0"
  }
}
